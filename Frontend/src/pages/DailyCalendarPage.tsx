import { useEffect, useState } from 'react'
import { errorMessage } from '../api/client'
import { calendarApi } from '../api/operations'
import type { DailySchedule } from '../types'

export function DailyCalendarPage(){const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[data,setData]=useState<DailySchedule|null>(null);const[error,setError]=useState('');useEffect(()=>{setError('');calendarApi.day(date).then(setData).catch(x=>setError(errorMessage(x)))},[date]);return <main className="workspace-page calendar-page"><header className="workspace-header"><div><p className="eyebrow">Manager / kalendar</p><h1>Dnevni raspored</h1><p>Svi termini, treneri, sale i prijavljeni klijenti za izabrani dan.</p></div><input className="date-picker" type="date" value={date} onChange={e=>setDate(e.target.value)}/></header>{error&&<div className="content-error">{error}</div>}<section className="calendar-timeline">{data?.appointments.map(appointment=><article key={appointment.id}><time>{appointment.startTime.slice(0,5)}<small>{appointment.endTime.slice(0,5)}</small></time><div className="timeline-dot"/><div className="progress-card"><div className="calendar-card-head"><div><p className="eyebrow">{appointment.session.type==='INDIVIDUAL'?'Individualni trening':'Grupni trening'}</p><h2>{appointment.room?.name??'Sala nije dodeljena'}</h2></div><span>{appointment.clients.length}/{appointment.session.maxParticipants}</span></div><dl><div><dt>Trener</dt><dd>{appointment.trainer?.email??'Nije dodeljen'}</dd></div><div><dt>Klijenti</dt><dd>{appointment.clients.map(c=>c.email).join(', ')||'Nema prijavljenih'}</dd></div></dl></div></article>)}{data&&!data.appointments.length&&<section className="progress-card empty-panel">Nema termina za izabrani dan.</section>}</section></main>}
